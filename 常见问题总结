1、内存溢出
在写脚本或是实现功能时，会有foreach循环批量获取数据，可能会造成内存溢出。解决循环批量获取数据造成的内存溢出，解决办法：分批处理，使用完后unset掉。
多进程拉取数据。可调整服务器分配给php的内存大小

unset()函数只能在变量值占用内存空间超过256字节时才会释放内存空间。只有当指向该变量的所有变量（如引用变量）都被销毁后（计数器=0），才会释放内存。
计数器永远！=0的情景： 数组获对象，有个指向自身的指针值，则永远不会变为0 

$mm=memory_get_usage(); //查看当前占用内存


